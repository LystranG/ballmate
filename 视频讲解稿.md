# 掌上约球 — 视频讲解稿（约5分钟）

---

## 第一部分：项目介绍（约30秒）

**【画面：打开项目首页/登录页】**

> 大家好，我今天演示的项目是"掌上约球"，这是一个面向校园和社区的球友召集平台。
>
> 它的核心功能就是让用户能快速找到附近想打球的人，发起邀约、凑齐人数一起运动。
>
> 技术栈方面，前端用的是 Vue 3 + Vant 4 组件库，后端用 Go 语言的 Gin 框架，数据库是 SQLite。

---

## 第二部分：用户注册与登录（约45秒）

**【操作：打开注册页面 `/register`】**

> 首先看用户系统。这是注册页面，用户需要填写用户名、密码、手机号这些基本信息。

**【操作：填写表单，点击注册按钮】**

> 注册成功后会自动跳转到登录页。

**【操作：在登录页输入刚才的账号密码，点击登录】**

> 登录这块用的是 JWT 认证。后端收到登录请求后，验证密码用的是 bcrypt 加密比对，通过后签发一个 JWT token 返回给前端。

**【操作：打开编辑器，翻到 `backend/utils/jwt.go` 和 `backend/service/auth.go`】**

> 这里可以看到，密码存储用了 bcrypt 哈希，不是明文存储。JWT token 有效期设置的7天，前端拿到 token 后存在 localStorage 里，每次请求通过 Authorization header 带上去。

**【操作：翻到 `backend/middleware/auth.go`】**

> 后端有一个认证中间件，每个需要登录的接口都会先经过这个中间件校验 token 是否有效。

---

## 第三部分：附近约球 — 首页（约1分钟）

**【操作：登录后进入首页 `/home`，展示附近邀约列表】**

> 登录之后进入首页，这就是"附近约球"功能。页面会先获取用户当前定位，然后请求后端接口拿到附近的邀约列表。

**【操作：点击不同的球类标签筛选，切换距离/时间排序】**

> 上面有球类筛选标签，可以按篮球、乒乓球等筛选。下面有排序切换，支持按距离优先或时间优先。

**【操作：打开编辑器，翻到 `backend/handler/invitation.go` 的 ListNearby 函数】**

> 距离计算这块比较有意思。因为用的是 SQLite，它不支持 MySQL 那种 RADIANS 函数，所以我在 SQL 里手动用 pi/180 做了 Haversine 公式的距离计算。如果 SQLite 数学函数不可用，还有一个 Go 层的回退方案，在内存里算距离再排序。

**【操作：回到手机页面，展示列表卡片信息】**

> 每张卡片显示了球类、时间、地点、当前人数和距离信息。

---

## 第四部分：发起邀约（约1分钟）

**【操作：点击底部 Tab "发起约球"，进入 `/create` 页面】**

> 接下来看发起邀约功能。用户可以选择球类、活动时间、地点和需要的人数。

**【操作：选择一个球类标签，点击时间选择器选时间】**

> 球类用标签选择，时间用 Vant 的 DatePicker 组件。

**【操作：点击地点输入框，弹出地图选点组件】**

> 地点选择这里我做了一个地图选点组件，用户可以在地图上点选位置，会自动获取经纬度和地址名称。

**【操作：设置人数，点击提交】**

> 设置好人数后提交，后端会创建一条邀约记录，状态默认是"等待中"。创建者自动算作第一个参与人，所以如果设置人数为1，创建后直接就是"已召集"状态。

**【操作：翻到 `backend/service/invitation.go` 的 CreateInvitation 部分】**

> 这里可以看到，创建邀约的同时会插入一条 participation 记录，把创建者加进去。如果 maxPeople 等于1，状态直接设为 gathered。

---

## 第五部分：加入邀约与参与人管理（约1分钟）

**【操作：回到首页，点击一条邀约进入详情页 `/invitation/:id`】**

> 点进一条邀约可以看到详情，包括球类、时间、地点、当前参与人数。

**【操作：点击底部"加入邀约"按钮】**

> 非发起人可以看到底部的"加入邀约"按钮，点击就能加入。加入后如果人数满了，状态会自动变成"已召集"。

**【操作：切换到发起人视角（或用另一个账号登录），进入自己发起的邀约详情】**

> 发起人进入自己的邀约详情，可以看到参与人列表，显示昵称和手机号。手机号中间4位用星号隐藏了，保护隐私。

**【操作：翻到 `backend/handler/invitation.go` 的 GetParticipants 函数】**

> 手机号脱敏是在后端做的，返回给前端之前就把中间4位替换成星号，前端直接展示就行。

**【操作：点击"终止邀约"按钮演示】**

> 发起人还可以终止或删除邀约。终止后状态变成"已终止"，其他人就看不到了。

---

## 第六部分：个人中心（约30秒）

**【操作：点击底部 Tab "我的"，进入个人中心 `/profile`】**

> 最后看个人中心。这里可以看到头像、昵称等基本信息。

**【操作：点击"我的邀约"，展示我发起的和我参与的两个 Tab】**

> "我的邀约"里分了两个 Tab，一个是我发起的，一个是我参与的，方便管理。

**【操作：返回，点击"编辑资料"，展示编辑页面】**

> 编辑资料可以改头像、昵称、常住地、球类兴趣这些。头像上传用的是 multipart/form-data，后端存到本地 uploads 目录。

**【操作：翻到 `backend/handler/user.go` 的 UploadAvatar 函数】**

> 文件上传做了前后端双重校验，前端限制文件类型和大小，后端也有扩展名白名单和大小限制，防止恶意文件上传。

---

## 第七部分：总结（约15秒）

**【画面：回到首页或展示项目结构】**

> 总结一下，这个项目实现了完整的用户系统、邀约发布与管理、附近约球浏览与加入、个人信息维护这些核心功能。前端 Vue 3 + Vant 4 做移动端适配，后端 Go + Gin 提供 RESTful API，数据库用 SQLite 轻量部署。整体是一个前后端分离的架构，通过 JWT 做认证鉴权。
>
> 以上就是我的项目演示，谢谢大家。

---

## 附：关键代码文件速查

| 功能 | 前端文件 | 后端文件 |
|------|----------|----------|
| 注册/登录 | `frontend/src/views/login/index.vue`, `register/index.vue` | `backend/handler/auth.go`, `backend/service/auth.go` |
| JWT认证 | `frontend/src/utils/request.js` (axios拦截器) | `backend/middleware/auth.go`, `backend/utils/jwt.go` |
| 附近约球 | `frontend/src/views/home/index.vue` | `backend/handler/invitation.go` (ListNearby) |
| 发起邀约 | `frontend/src/views/create/index.vue` | `backend/service/invitation.go` (CreateInvitation) |
| 邀约详情 | `frontend/src/views/invitation/detail.vue` | `backend/handler/invitation.go` (GetInvitation) |
| 地图选点 | `frontend/src/components/MapPicker.vue` | — |
| 个人中心 | `frontend/src/views/profile/index.vue`, `edit.vue` | `backend/handler/user.go` |
| 头像上传 | `frontend/src/views/profile/edit.vue` | `backend/handler/user.go` (UploadAvatar) |

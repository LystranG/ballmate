---
phase: 02-invite-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/api/invitation.js
  - frontend/src/components/MapPicker.vue
  - frontend/src/views/create/index.vue
  - frontend/src/router/index.js
autonomous: true
requirements:
  - INVITE-01
  - INVITE-02
  - INVITE-03

must_haves:
  truths:
    - "用户可以在 /create 页面选择球类（单选 Tag）、活动时间（PickerGroup）、地点（地图选点）、人数（预设档位）"
    - "地图选点组件在用户点击地图后显示逆地理编码地址，并向父组件 emit { longitude, latitude, address }"
    - "表单提交后调用 POST /api/v1/invitations，成功后 toast '发布成功' 并跳转到 /invitation/:id"
    - "四个字段均未填写时提交，显示对应校验错误提示"
    - "/create 路由已注册，TabBar 第二项（发起约球）点击可到达"
  artifacts:
    - path: "frontend/src/api/invitation.js"
      provides: "邀约 API 封装（createInvitation, getInvitation, terminateInvitation, deleteInvitation, getParticipants）"
      contains: "export function createInvitation"
    - path: "frontend/src/components/MapPicker.vue"
      provides: "高德地图选点组件，emit select 事件携带 { longitude, latitude, address }"
      contains: "AMapLoader"
    - path: "frontend/src/views/create/index.vue"
      provides: "发起邀约表单页，包含球类/时间/地点/人数四个字段"
      contains: "MapPicker"
    - path: "frontend/src/router/index.js"
      provides: "/create 和 /invitation/:id 路由注册"
      contains: "path: '/create'"
  key_links:
    - from: "frontend/src/views/create/index.vue"
      to: "frontend/src/components/MapPicker.vue"
      via: "import MapPicker + @select 事件监听"
      pattern: "MapPicker"
    - from: "frontend/src/views/create/index.vue"
      to: "frontend/src/api/invitation.js"
      via: "createInvitation(formData)"
      pattern: "createInvitation"
    - from: "frontend/src/components/MapPicker.vue"
      to: "@amap/amap-jsapi-loader"
      via: "AMapLoader.load({ key, version: '2.0', plugins: ['AMap.Geocoder'] })"
      pattern: "AMapLoader"
---

<objective>
实现发起邀约垂直切片：前端 API 封装、地图选点组件、发起邀约表单页，以及路由注册。

Purpose: 用户完成从打开 /create 页面到成功发布邀约的完整流程（UI→API→后端）。

Output: 可用的发起邀约页面，包含球类/时间/地点/人数四个字段，地图选点组件，表单提交后跳转详情页。
</objective>

<execution_context>
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-invite-core/02-CONTEXT.md
@.planning/phases/02-invite-core/02-RESEARCH.md
@.planning/phases/02-invite-core/02-UI-SPEC.md
@.planning/phases/02-invite-core/02-01-SUMMARY.md

<interfaces>
<!-- 已有前端模式，executor 直接复用 -->

From frontend/src/api/request.js:
```javascript
// Axios 封装，已处理 token 注入和 401 拦截
// 响应拦截器直接返回 response.data（即后端 {code, message, data}）
import request from './request'
export function someApi(data) {
  return request.post('/endpoint', data)
}
```

From frontend/src/api/user.js（API 封装模式）:
```javascript
export function getProfile() { return request.get('/user/profile') }
export function updateProfile(data) { return request.put('/user/profile', data) }
```

From frontend/src/router/index.js（路由注册模式）:
```javascript
{ path: '/profile', component: () => import('@/views/profile/index.vue'), meta: { auth: true } }
```

From frontend/src/components/TabBar.vue:
```javascript
// TabBar 已预留 /create 路由，to="/create"，active index = 1
const routeMap = { '/': 0, '/create': 1, '/profile': 2 }
```

AMap 集成关键点（from 02-RESEARCH.md Pitfall 1 + 2）:
- 地图实例必须用 shallowRef()，不能用 ref()（Vue Proxy 破坏 AMap 对象）
- AMapLoader.load 前必须设置 window._AMapSecurityConfig = { securityJsCode: 'YOUR_SECURITY_KEY' }
- AMap Key 和 securityJsCode 从环境变量读取：import.meta.env.VITE_AMAP_KEY 和 import.meta.env.VITE_AMAP_SECURITY_KEY
- 若环境变量未配置，组件显示提示文字"地图加载失败，请配置高德地图 Key"而非崩溃

Vant PickerGroup 模式（from 02-RESEARCH.md Pattern 3）:
```vue
<van-popup v-model:show="showPicker" position="bottom">
  <van-picker-group title="选择活动时间" :tabs="['日期', '时间']"
    @confirm="onConfirm" @cancel="showPicker = false">
    <van-date-picker v-model="selectedDate" :min-date="new Date()" />
    <van-time-picker v-model="selectedTime" :columns-type="['hour', 'minute']" />
  </van-picker-group>
</van-popup>
```

UI-SPEC 球类列表（from 02-UI-SPEC.md + 01-CONTEXT.md D-02/D-04）:
- 球类选项：['篮球', '足球', '羽毛球', '乒乓球', '网球', '排球', '棒球', '橄榄球']
- 人数档位：[2, 4, 6, 8, 10]，显示为 "N人"
- 选中 Tag：type="primary"，未选中：plain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建前端 API 封装 + 路由注册</name>
  <files>frontend/src/api/invitation.js, frontend/src/router/index.js</files>
  <read_first>
    - frontend/src/api/user.js — 复用 API 封装模式（import request + export function）
    - frontend/src/api/request.js — 确认 baseURL 为 /api/v1
    - frontend/src/router/index.js — 在现有路由数组中追加新路由，保持守卫逻辑不变
    - .planning/phases/02-invite-core/02-RESEARCH.md — 前端 API 封装示例（Code Examples 章节）
  </read_first>
  <action>
    创建 frontend/src/api/invitation.js，导出五个函数：
    - createInvitation(data)：POST /invitations，data 包含 sport_type, activity_time（ISO 8601 字符串）, address, latitude, longitude, max_people
    - getInvitation(id)：GET /invitations/${id}
    - terminateInvitation(id)：PUT /invitations/${id}/terminate
    - deleteInvitation(id)：DELETE /invitations/${id}
    - getParticipants(id)：GET /invitations/${id}/participants

    修改 frontend/src/router/index.js，在 routes 数组中追加两条路由：
    - { path: '/create', component: () => import('@/views/create/index.vue'), meta: { auth: true } }
    - { path: '/invitation/:id', component: () => import('@/views/invitation/detail.vue'), meta: { auth: true } }
    保持现有路由和导航守卫逻辑不变。
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/frontend && node -e "const fs=require('fs'); const r=fs.readFileSync('src/router/index.js','utf8'); const a=fs.readFileSync('src/api/invitation.js','utf8'); console.log(r.includes('/create') && r.includes('/invitation/:id') && a.includes('createInvitation') && a.includes('getParticipants') ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/invitation.js 包含 `export function createInvitation`、`export function getInvitation`、`export function terminateInvitation`、`export function deleteInvitation`、`export function getParticipants`
    - frontend/src/router/index.js 包含 `path: '/create'` 和 `path: '/invitation/:id'`
    - 两条新路由均有 `meta: { auth: true }`
    - 验证脚本输出 "OK"
  </acceptance_criteria>
  <done>invitation.js API 封装创建完成，/create 和 /invitation/:id 路由已注册</done>
</task>

<task type="auto">
  <name>Task 2: 创建 MapPicker 组件 + 发起邀约表单页</name>
  <files>frontend/src/components/MapPicker.vue, frontend/src/views/create/index.vue</files>
  <read_first>
    - frontend/src/components/TabBar.vue — 复用组件引入和 script setup 模式
    - frontend/src/views/profile/edit.vue — 复用 van-form + van-field + van-nav-bar 布局模式
    - frontend/src/api/invitation.js — 确认 createInvitation 函数签名（Task 1 刚创建）
    - .planning/phases/02-invite-core/02-RESEARCH.md — Pattern 2（MapPicker 组件）、Pattern 3（PickerGroup）、Pitfall 1（shallowRef）、Pitfall 2（securityJsCode）
    - .planning/phases/02-invite-core/02-UI-SPEC.md — Screen 1 组件清单、布局结构、Copywriting Contract、Interaction Contracts
    - .planning/phases/02-invite-core/02-CONTEXT.md — D-01（地图选点）、D-02（PickerGroup）、D-03（人数档位）、D-04（球类单选）
  </read_first>
  <action>
    创建 frontend/src/components/MapPicker.vue。

    使用 script setup + shallowRef 存储地图实例（不能用 ref，per Pitfall 1）。在 onMounted 中：先检查 import.meta.env.VITE_AMAP_KEY 是否存在，若不存在则设置 mapError.value = true 并 return（显示错误提示而非崩溃）。若 Key 存在，设置 window._AMapSecurityConfig = { securityJsCode: import.meta.env.VITE_AMAP_SECURITY_KEY }（per Pitfall 2），然后调用 AMapLoader.load({ key: import.meta.env.VITE_AMAP_KEY, version: '2.0', plugins: ['AMap.Geocoder'] })。地图初始化 zoom: 14，center 默认 [116.397428, 39.90923]（北京，fallback）。监听地图 click 事件，获取 e.lnglat 的 lng/lat，调用 AMap.Geocoder 的 getAddress 逆地理编码，成功后更新 address ref 并 emit('select', { longitude: lng, latitude: lat, address })。在 onUnmounted 中调用 map.value?.destroy()。

    模板结构：若 mapError 为 true，显示 van-empty description="地图加载失败，请配置高德地图 Key"；否则显示 div#map-container（height: 300px）+ van-field（v-model="address" readonly placeholder="点击地图选择地点"）。

    创建 frontend/src/views/create/index.vue。

    使用 script setup，导入 createInvitation from '@/api/invitation'，导入 MapPicker from '@/components/MapPicker.vue'，导入 useRouter from 'vue-router'，导入 showToast from 'vant'。

    响应式数据：selectedSport（ref('')），showTimePicker（ref(false)），selectedDate（ref([]，PickerGroup 需要数组格式）），selectedTime（ref([])），activityTimeDisplay（ref('')），location（ref({ address: '', latitude: 0, longitude: 0 })），selectedCapacity（ref(0)），loading（ref(false)）。

    球类列表常量：SPORTS = ['篮球', '足球', '羽毛球', '乒乓球', '网球', '排球', '棒球', '橄榄球']。
    人数档位常量：CAPACITIES = [2, 4, 6, 8, 10]。

    onTimeConfirm 函数：接收 PickerGroup confirm 事件的 { selectedValues }，将 selectedDate 和 selectedTime 的值拼接为 "YYYY-MM-DDTHH:mm:00" 格式的 ISO 字符串存入 activityTimeISO ref，同时格式化为 "MM月DD日 HH:mm" 存入 activityTimeDisplay（per UI-SPEC Copywriting Contract）。

    onMapSelect 函数：接收 MapPicker emit 的 { longitude, latitude, address }，更新 location ref。

    onSubmit 函数：校验 selectedSport、activityTimeISO、location.address、selectedCapacity 均非空，任一为空时 showToast 对应错误文字（per UI-SPEC Copywriting Contract：请选择球类/请选择活动时间/请选择活动地点/请选择人数）。校验通过后设 loading = true，调用 createInvitation({ sport_type: selectedSport, activity_time: activityTimeISO, address: location.address, latitude: location.latitude, longitude: location.longitude, max_people: selectedCapacity })，成功后 showToast('发布成功') 并 router.push('/invitation/' + data.id)，失败时 showToast('提交失败，请重试')，finally 设 loading = false。

    模板结构（per UI-SPEC Screen 1 布局）：
    - van-nav-bar title="发起约球" left-arrow @click-left="router.back()"
    - van-cell-group inset，内含：
      - 球类选择 section：label "球类" + flex wrap 的 van-tag 列表（SPORTS 遍历，选中 type="primary"，未选中 plain，@click 设置 selectedSport）
      - van-field label="活动时间" v-model="activityTimeDisplay" is-link readonly placeholder="请选择活动时间" @click="showTimePicker=true"
      - van-field label="活动地点" v-model="location.address" is-link readonly placeholder="请选择活动地点"（点击展开 MapPicker，用 van-popup 或直接内嵌）
      - 人数选择 section：label "人数" + flex wrap 的 van-tag 列表（CAPACITIES 遍历，显示 N+"人"，选中 type="primary"，未选中 plain）
    - van-popup position="bottom" v-model:show="showTimePicker"，内含 van-picker-group（title="选择活动时间" :tabs="['日期', '时间']" @confirm="onTimeConfirm" @cancel="showTimePicker=false"）+ van-date-picker（:min-date="new Date()"）+ van-time-picker（:columns-type="['hour', 'minute']"）
    - 地图选点：用 van-popup position="bottom" 包裹 MapPicker 组件，@select="onMapSelect"，popup 内有确认按钮关闭 popup
    - 提交按钮 wrapper（padding: 24px 16px）：van-button type="primary" block round :loading="loading" @click="onSubmit" 文字"发布邀约"
    - TabBar 组件（active index = 1）
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/MapPicker.vue 包含 `shallowRef` 和 `AMapLoader` 和 `emit('select'`
    - frontend/src/components/MapPicker.vue 包含 `VITE_AMAP_KEY` 环境变量检查逻辑
    - frontend/src/views/create/index.vue 包含 `MapPicker` 组件引用
    - frontend/src/views/create/index.vue 包含 `createInvitation` 调用
    - frontend/src/views/create/index.vue 包含 `van-picker-group` 和 `van-date-picker` 和 `van-time-picker`
    - frontend/src/views/create/index.vue 包含球类列表 `SPORTS` 和人数档位 `CAPACITIES`
    - `npm run build` 在 frontend/ 目录退出码为 0（无编译错误）
  </acceptance_criteria>
  <done>MapPicker 组件和发起邀约表单页创建完成，npm run build 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入→表单提交 | 前端校验四个字段非空，后端 handler 做二次校验 |
| 浏览器→高德 API | AMap Key 通过环境变量注入，不硬编码在源码中 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | VITE_AMAP_KEY 暴露 | accept | 前端 JS API Key 在浏览器端本质上可见，高德控制台配置域名白名单限制滥用 |
| T-02-07 | Tampering | 表单提交伪造 max_people | mitigate | 后端 handler 校验 max_people 在 [2,4,6,8,10] 范围内，拒绝非法值 |
| T-02-08 | Tampering | 表单提交伪造 activity_time（过去时间） | mitigate | 后端 handler 校验 activity_time > time.Now()，拒绝过去时间 |
</threat_model>

<verification>
前端整体验证：
1. `npm run build` 在 frontend/ 目录通过，无编译错误
2. 访问 /create 页面，TabBar 第二项高亮
3. 球类 Tag 单选交互正常（选中变蓝，再选其他取消前一个）
4. 点击活动时间字段，PickerGroup 弹出，选择后字段显示 "MM月DD日 HH:mm" 格式
5. 点击活动地点字段，地图 popup 弹出（若无 AMap Key，显示错误提示而非白屏）
6. 人数 Tag 单选交互正常
7. 未填写字段时点击发布，显示对应 toast 错误提示
</verification>

<success_criteria>
1. `npm run build` 通过，无编译错误
2. /create 路由可访问，TabBar 第二项正确高亮
3. 四个表单字段均有交互（球类单选、时间 PickerGroup、地图选点、人数单选）
4. 表单提交调用 POST /api/v1/invitations，成功后跳转 /invitation/:id
</success_criteria>

<output>
创建 `.planning/phases/02-invite-core/02-02-SUMMARY.md`
</output>

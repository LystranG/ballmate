---
phase: 03-nearby-and-my-invitations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/service/invitation.go
  - backend/handler/invitation.go
  - backend/router/router.go
  - frontend/src/composables/useGeolocation.js
  - frontend/src/utils/format.js
  - frontend/src/api/invitation.js
  - frontend/src/components/InvitationCard.vue
  - frontend/src/views/home/index.vue
autonomous: true
requirements:
  - NEARBY-01
  - NEARBY-02
  - NEARBY-03
  - NEARBY-04
  - NEARBY-05

must_haves:
  truths:
    - "GET /api/v1/invitations/nearby?lat=X&lng=Y 返回按距离排序的邀约列表，每条含 distance 字段"
    - "sort_by=time 参数切换为按活动时间排序"
    - "sport_type=篮球 参数筛选指定球类"
    - "浏览器定位成功后自动加载附近邀约列表"
    - "定位失败时弹出地图选点组件让用户手动选择位置"
    - "列表支持上拉加载更多（Vant List）"
    - "顶部球类标签栏可切换筛选"
  artifacts:
    - path: "backend/service/invitation.go"
      provides: "ListNearbyInvitations 函数（Haversine 距离计算 + 排序 + 筛选 + 分页）"
      contains: "func ListNearbyInvitations"
    - path: "backend/handler/invitation.go"
      provides: "ListNearby handler（绑定 query 参数，调用 service）"
      contains: "func ListNearby"
    - path: "backend/router/router.go"
      provides: "GET /nearby 路由注册"
      contains: "invitation.GET(\"/nearby\""
    - path: "frontend/src/composables/useGeolocation.js"
      provides: "useGeolocation composable（定位 + 错误处理）"
      contains: "export function useGeolocation"
    - path: "frontend/src/utils/format.js"
      provides: "formatDistance 工具函数"
      contains: "export function formatDistance"
    - path: "frontend/src/components/InvitationCard.vue"
      provides: "邀约卡片组件（球类、地点、时间、人数、距离/状态）"
      contains: "InvitationCard"
    - path: "frontend/src/views/home/index.vue"
      provides: "首页改造为附近邀约列表（定位 + 筛选 + 排序 + 无限滚动）"
      contains: "van-list"
  key_links:
    - from: "frontend/src/views/home/index.vue"
      to: "/api/v1/invitations/nearby"
      via: "getNearbyInvitations API 调用"
      pattern: "getNearbyInvitations"
    - from: "frontend/src/views/home/index.vue"
      to: "frontend/src/composables/useGeolocation.js"
      via: "import useGeolocation"
      pattern: "useGeolocation"
    - from: "backend/handler/invitation.go"
      to: "backend/service/invitation.go"
      via: "service.ListNearbyInvitations"
      pattern: "service\\.ListNearbyInvitations"
---

<objective>
实现附近邀约列表完整垂直切片：后端距离查询 API + 前端定位、筛选、排序、无限滚动列表。

Purpose: 用户打开首页即可发现附近的球类邀约，按距离或时间排序，按球类筛选。
Output: 可用的首页附近邀约列表，含定位、卡片展示、筛选标签、排序切换、上拉加载。
</objective>

<execution_context>
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-nearby-and-my-invitations/03-CONTEXT.md
@.planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md

<interfaces>
<!-- 已有后端模式，executor 直接复用 -->

From backend/service/invitation.go:
```go
var DB *gorm.DB

type InvitationResponse struct {
    ID               uint      `json:"id"`
    CreatorID        uint      `json:"creator_id"`
    SportType        string    `json:"sport_type"`
    ActivityTime     time.Time `json:"activity_time"`
    Address          string    `json:"address"`
    Latitude         float64   `json:"latitude"`
    Longitude        float64   `json:"longitude"`
    MaxPeople        int       `json:"max_people"`
    Status           string    `json:"status"`
    ParticipantCount int       `json:"participant_count"`
    CreatedAt        time.Time `json:"created_at"`
}

func computeStatus(inv model.Invitation) string
func buildResponse(inv model.Invitation) *InvitationResponse
```

From backend/handler/invitation.go:
```go
// handler 模式：c.GetUint("userID")、ShouldBindQuery/ShouldBindJSON、Success/Error
func CreateInvitation(c *gin.Context)
```

From backend/handler/response.go:
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpCode int, message string)
```

From backend/router/router.go:
```go
invitation := api.Group("/invitations")
invitation.Use(middleware.AuthMiddleware())
```

From frontend/src/api/invitation.js:
```javascript
import request from './request'
export function getInvitation(id) { return request.get(`/invitations/${id}`) }
```

From frontend/src/components/MapPicker.vue:
```
// 高德地图选点组件，emit 'confirm' 事件返回 { lat, lng, address }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 ListNearby API（Haversine 距离查询 + 排序 + 筛选 + 分页）</name>
  <files>backend/service/invitation.go, backend/handler/invitation.go, backend/router/router.go</files>
  <read_first>
    - backend/service/invitation.go — 已有 InvitationResponse、computeStatus、buildResponse、DB 变量
    - backend/handler/invitation.go — 已有 handler 模式（ShouldBindQuery、Success/Error）
    - backend/router/router.go — 已有 invitation 路由组
    - .planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md — Pattern 1（Haversine SQL）、Code Examples（ListNearby handler）
  </read_first>
  <action>
    在 backend/service/invitation.go 中新增：

    1. 定义 NearbyInvitationResponse 结构体，嵌入 InvitationResponse 所有字段 + Distance float64 json:"distance"。

    2. 实现 ListNearbyInvitations(userID uint, lat, lng float64, sportType, sortBy string, page, pageSize int) ([]NearbyInvitationResponse, int64, error)：
       - 排除当前用户自己创建的邀约（per NEARBY-02 "其他人发出的"）
       - Haversine SQL 表达式计算 distance（公里），使用 sin/cos/asin/sqrt/pow + 手动 *pi/180 替代 RADIANS（per Pitfall 1）
       - 如果 SQLite 数学函数不可用的回退：先查出所有邀约，Go 层用 haversineDistance 计算距离再排序分页
       - sport_type 非空时 WHERE sport_type = ?
       - sortBy == "time" 时 ORDER BY activity_time ASC，否则 ORDER BY distance ASC（per D-05, NEARBY-03, NEARBY-04）
       - OFFSET/LIMIT 分页
       - 不限制距离范围（per D-08）
       - 对每条结果调用 computeStatus 计算实际状态，查询 participant_count
       - 返回列表和 total 总数

    3. 实现辅助函数 haversineDistance(lat1, lng1, lat2, lng2 float64) float64，使用 Go math 包计算两点距离（公里），作为 SQL 不可用时的回退。

    在 backend/handler/invitation.go 中新增：

    4. 定义 listNearbyRequest 结构体，字段：Latitude float64 form:"lat" binding:"required"，Longitude float64 form:"lng" binding:"required"，Page int form:"page,default=1"，PageSize int form:"page_size,default=10"，SortBy string form:"sort_by,default=distance"，SportType string form:"sport_type"。

    5. 实现 ListNearby(c *gin.Context)：c.GetUint("userID")，ShouldBindQuery 绑定参数，PageSize 上限 20，调用 service.ListNearbyInvitations，Success 返回 gin.H{"list": list, "total": total}。

    在 backend/router/router.go 中：

    6. 在 invitation 路由组内（现有路由之前）新增：invitation.GET("/nearby", handler.ListNearby)。注意 /nearby 必须在 /:id 之前注册，否则 Gin 会把 "nearby" 当作 id 参数匹配。
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/backend && go build ./... 2>&1 && echo "BUILD_OK"</automated>
  </verify>
  <done>ListNearby API 编译通过，路由注册在 /:id 之前，service 含 Haversine 距离计算逻辑</done>
</task>

<task type="auto">
  <name>Task 2: 前端首页改造（定位 + 邀约卡片 + 筛选标签 + 排序 + 无限滚动）</name>
  <files>frontend/src/composables/useGeolocation.js, frontend/src/utils/format.js, frontend/src/api/invitation.js, frontend/src/components/InvitationCard.vue, frontend/src/views/home/index.vue</files>
  <read_first>
    - frontend/src/views/home/index.vue — 当前空占位页，需完全重写
    - frontend/src/api/invitation.js — 已有接口，需追加 getNearbyInvitations
    - frontend/src/components/MapPicker.vue — 定位失败时复用的地图选点组件
    - frontend/src/views/invitation/detail.vue — 了解 statusConfig 和 formatTime 模式
    - .planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md — Pattern 2（useGeolocation）、Pattern 3（Vant List）、Pitfall 3（v-if 控制 List 渲染时机）
  </read_first>
  <action>
    1. 创建 frontend/src/composables/ 目录和 useGeolocation.js：
       - 导出 useGeolocation() composable
       - 返回 { latitude, longitude, error, loading, getPosition }
       - getPosition 返回 Promise，成功 resolve { lat, lng }，失败 reject 错误码
       - 错误码映射：1→PERMISSION_DENIED, 2→POSITION_UNAVAILABLE, 3→TIMEOUT
       - 选项：enableHighAccuracy: true, timeout: 10000, maximumAge: 300000

    2. 创建 frontend/src/utils/format.js：
       - 导出 formatDistance(distanceKm)：< 1km 显示 Math.round(km*1000) + "m"，>= 1km 显示 toFixed(1) + "km"（per D-07）

    3. 在 frontend/src/api/invitation.js 追加：
       - export function getNearbyInvitations(params) { return request.get('/invitations/nearby', { params }) }

    4. 创建 frontend/src/components/InvitationCard.vue：
       - Props: invitation (Object, required), mode (String, default: 'distance'，可选 'status')
       - 卡片展示：球类图标/名称、地点（address 截断）、活动时间（MM月DD日 HH:mm）、人数进度（participant_count/max_people）、距离（mode=distance 时用 formatDistance）或状态标签（mode=status 时显示等待中/已召集/已终止/已过期）（per D-01, D-15）
       - 不可加入的邀约（status 为 gathered/terminated/expired）整体置灰（per D-04）
       - 使用 Vant Card 或自定义 div 布局，emit click 事件
       - 中文注释说明 mode 参数用途

    5. 重写 frontend/src/views/home/index.vue：
       - 页面结构：NavBar（标题"附近约球"）+ 顶部球类标签横向滚动栏 + 排序切换 + Vant List + TabBar
       - 球类标签栏：使用 Vant Tabs 或自定义横向滚动 div，选项为 全部/篮球/足球/羽毛球/乒乓球/网球/排球，点击切换重置列表重新加载（per D-03）
       - 排序切换：距离优先/时间优先 两个按钮或 Vant DropdownMenu，切换后重置列表（per NEARBY-04，Claude's Discretion）
       - 定位逻辑：onMounted 调用 useGeolocation().getPosition()，成功后设置 hasLocation=true 渲染 List；失败时显示提示并弹出 MapPicker 让用户手动选点（per D-06），MapPicker confirm 后设置坐标和 hasLocation
       - Vant List：v-if="hasLocation" 控制渲染（per Pitfall 3），v-model:loading, :finished, @load="onLoad"，finished-text="没有更多了"
       - onLoad：调用 getNearbyInvitations({ lat, lng, page, page_size: 10, sort_by, sport_type })，push 到 list，page++，list.length >= total 时 finished=true
       - 卡片点击跳转 /invitation/:id
       - 空状态：Vant Empty 组件（per Claude's Discretion）
       - 保留底部 TabBar 组件
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/frontend && npx vue-tsc --noEmit 2>&1 || npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>首页展示附近邀约列表，支持定位/手动选点、球类筛选、距离/时间排序、上拉加载更多，卡片显示球类+地点+时间+人数+距离</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→API | 前端传 lat/lng 参数，后端不信任前端坐标（仅用于距离计算，不存储） |
| API→数据库 | GORM 参数化查询，Haversine SQL 中 lat/lng 作为绑定参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GET /invitations/nearby | accept | 列表仅返回公开邀约信息（球类、地点、时间、人数），不含创建者手机号等敏感信息 |
| T-03-02 | Denial of Service | GET /invitations/nearby（大量请求） | mitigate | PageSize 上限 20，防止单次请求返回过多数据 |
| T-03-03 | Tampering | lat/lng 参数伪造 | accept | 用户伪造坐标仅影响自己看到的距离排序，不影响其他用户，无安全风险 |
</threat_model>

<verification>
1. `cd backend && go build ./...` 编译通过
2. 启动后端，`curl "http://localhost:8080/api/v1/invitations/nearby?lat=30.5&lng=104.0" -H "Authorization: Bearer <token>"` 返回 200 + {list, total}
3. 前端构建无错误
4. 浏览器打开首页，允许定位后显示邀约列表（或无邀约时显示空状态）
5. 切换球类标签，列表按筛选条件刷新
6. 切换排序方式，列表重新排序
</verification>

<success_criteria>
1. 后端 GET /invitations/nearby 接口返回含 distance 字段的邀约列表
2. 前端首页完成定位后自动加载附近邀约
3. 球类标签筛选和排序切换功能正常
4. 上拉加载更多正常工作
5. 定位失败时可通过地图选点手动设置位置
</success_criteria>

<output>
创建 `.planning/phases/03-nearby-and-my-invitations/03-01-SUMMARY.md`
</output>

---
phase: 03-nearby-and-my-invitations
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/service/invitation.go
  - backend/handler/invitation.go
  - backend/router/router.go
  - frontend/src/api/invitation.js
  - frontend/src/views/my-invitations/index.vue
  - frontend/src/views/profile/index.vue
  - frontend/src/router/index.js
autonomous: true
requirements:
  - MY-01
  - MY-02

must_haves:
  truths:
    - "GET /api/v1/invitations/my?type=created 返回当前用户发起的邀约列表"
    - "GET /api/v1/invitations/my?type=joined 返回当前用户参与的邀约列表（排除自己创建的）"
    - "我的邀约页面有两个 Tab：我发起的 / 我参与的"
    - "卡片显示状态标签而非距离"
    - "从个人中心菜单可进入我的邀约页面"
  artifacts:
    - path: "backend/service/invitation.go"
      provides: "MyInvitations 函数（created/joined 两种查询）"
      contains: "func MyInvitations"
    - path: "backend/handler/invitation.go"
      provides: "MyInvitations handler"
      contains: "func MyInvitations"
    - path: "backend/router/router.go"
      provides: "GET /my 路由注册"
      contains: "invitation.GET(\"/my\""
    - path: "frontend/src/views/my-invitations/index.vue"
      provides: "我的邀约页面（Tabs + List + InvitationCard status 模式）"
      contains: "van-tabs"
    - path: "frontend/src/views/profile/index.vue"
      provides: "个人中心新增我的邀约入口"
      contains: "我的邀约"
    - path: "frontend/src/router/index.js"
      provides: "/my-invitations 路由注册"
      contains: "/my-invitations"
  key_links:
    - from: "frontend/src/views/my-invitations/index.vue"
      to: "/api/v1/invitations/my"
      via: "getMyInvitations API 调用"
      pattern: "getMyInvitations"
    - from: "frontend/src/views/profile/index.vue"
      to: "frontend/src/views/my-invitations/index.vue"
      via: "router.push('/my-invitations')"
      pattern: "my-invitations"
---

<objective>
实现我的邀约完整垂直切片：后端 My Invitations API + 前端 Tab 切换页面 + 个人中心入口。

Purpose: 用户可以查看自己发起和参与的所有邀约，方便管理和回顾。
Output: 可用的我的邀约页面，含 Tab 切换、状态标签卡片、无限滚动，从个人中心可进入。
</objective>

<execution_context>
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/workDir/school/移动开发/期末/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-nearby-and-my-invitations/03-CONTEXT.md
@.planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md
@.planning/phases/03-nearby-and-my-invitations/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的组件，本 plan 复用 -->

From frontend/src/components/InvitationCard.vue (Plan 01 创建):
```vue
<!-- Props: invitation (Object), mode (String: 'distance' | 'status') -->
<!-- mode='status' 时显示状态标签而非距离 -->
```

From backend/service/invitation.go:
```go
var DB *gorm.DB

type InvitationResponse struct {
    ID               uint      `json:"id"`
    CreatorID        uint      `json:"creator_id"`
    SportType        string    `json:"sport_type"`
    ActivityTime     time.Time `json:"activity_time"`
    Address          string    `json:"address"`
    Latitude         float64   `json:"latitude"`
    Longitude        float64   `json:"longitude"`
    MaxPeople        int       `json:"max_people"`
    Status           string    `json:"status"`
    ParticipantCount int       `json:"participant_count"`
    CreatedAt        time.Time `json:"created_at"`
}

func computeStatus(inv model.Invitation) string
func buildResponse(inv model.Invitation) *InvitationResponse
```

From frontend/src/views/profile/index.vue:
```vue
<!-- 菜单区域使用 van-cell-group + van-cell is-link -->
<van-cell title="编辑资料" is-link @click="router.push('/profile/edit')" />
```

From frontend/src/router/index.js:
```javascript
// 路由注册模式：{ path, component: () => import(...), meta: { auth: true } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 MyInvitations API（created/joined 查询 + 分页）</name>
  <files>backend/service/invitation.go, backend/handler/invitation.go, backend/router/router.go</files>
  <read_first>
    - backend/service/invitation.go — 已有 buildResponse、computeStatus、DB
    - backend/handler/invitation.go — 已有 handler 模式
    - backend/router/router.go — 已有 invitation 路由组
    - .planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md — Code Examples（MyInvitations service）
  </read_first>
  <action>
    在 backend/service/invitation.go 中新增：

    1. 实现 MyInvitations(userID uint, listType string, page, pageSize int) ([]InvitationResponse, int64, error)：
       - listType == "created"：WHERE creator_id = userID，ORDER BY created_at DESC，OFFSET/LIMIT 分页
       - listType == "joined"：JOIN participations ON participations.invitation_id = invitations.id，WHERE participations.user_id = userID AND participations.deleted_at IS NULL AND invitations.creator_id != userID（排除自己创建的），ORDER BY invitations.created_at DESC
       - 分别 COUNT 获取 total
       - 对每条结果调用 buildResponse（含 computeStatus 和 participant_count）
       - 返回 responses 和 total

    在 backend/handler/invitation.go 中新增：

    2. 定义 myInvitationsRequest 结构体：Type string form:"type,default=created"（created | joined），Page int form:"page,default=1"，PageSize int form:"page_size,default=10"。

    3. 实现 MyInvitations(c *gin.Context)：c.GetUint("userID")，ShouldBindQuery 绑定参数，PageSize 上限 20，调用 service.MyInvitations，Success 返回 gin.H{"list": list, "total": total}。

    在 backend/router/router.go 中：

    4. 在 invitation 路由组内新增（放在 /nearby 之后、/:id 之前）：invitation.GET("/my", handler.MyInvitations)。注意 /my 必须在 /:id 之前注册。
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/backend && go build ./... 2>&1 && echo "BUILD_OK"</automated>
  </verify>
  <done>MyInvitations API 编译通过，支持 type=created 和 type=joined 两种查询，路由注册在 /:id 之前</done>
</task>

<task type="auto">
  <name>Task 2: 前端我的邀约页面 + 个人中心入口 + 路由注册</name>
  <files>frontend/src/api/invitation.js, frontend/src/views/my-invitations/index.vue, frontend/src/views/profile/index.vue, frontend/src/router/index.js</files>
  <read_first>
    - frontend/src/views/profile/index.vue — 需在菜单中新增"我的邀约"入口
    - frontend/src/router/index.js — 需新增 /my-invitations 路由
    - frontend/src/api/invitation.js — 需追加 getMyInvitations
    - frontend/src/components/InvitationCard.vue — Plan 01 创建的卡片组件，mode="status" 用法
    - .planning/phases/03-nearby-and-my-invitations/03-RESEARCH.md — Pattern 4（Vant Tabs 切换独立数据）
  </read_first>
  <action>
    1. 在 frontend/src/api/invitation.js 追加：
       - export function getMyInvitations(params) { return request.get('/invitations/my', { params }) }

    2. 创建 frontend/src/views/my-invitations/ 目录和 index.vue：
       - NavBar：标题 "我的邀约"，左箭头返回
       - Vant Tabs v-model:active="activeTab"，两个 Tab：title="我发起的" 和 title="我参与的"
       - 每个 Tab 内独立的 Vant List（v-model:loading, :finished, @load）
       - 使用 InvitationCard 组件，mode="status"（显示状态标签而非距离，per D-15）
       - 卡片点击跳转 /invitation/:id
       - 空状态：Vant Empty description="暂无发起的邀约" / "暂无参与的邀约"
       - Tab 切换时不重置另一个 Tab 的数据（Vant Tabs lazy-render 默认行为）
       - 每个 Tab 独立的 list/loading/finished/page 状态
       - onLoad 调用 getMyInvitations({ type: 'created'/'joined', page, page_size: 10 })

    3. 修改 frontend/src/views/profile/index.vue：
       - 在 menu-group 的 van-cell-group 中，"编辑资料" 之前新增：
         <van-cell title="我的邀约" is-link @click="router.push('/my-invitations')" />
       （per D-14）

    4. 修改 frontend/src/router/index.js：
       - 新增路由 { path: '/my-invitations', component: () => import('@/views/my-invitations/index.vue'), meta: { auth: true } }
       - 放在 /profile 路由之后
  </action>
  <verify>
    <automated>cd /Users/lystran/workDir/school/移动开发/期末/frontend && npx vue-tsc --noEmit 2>&1 || npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>我的邀约页面含 Tab 切换和状态卡片列表，个人中心有入口，路由已注册</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→API | JWT 认证确保只能查看自己的邀约列表 |
| API→数据库 | WHERE creator_id = userID / participations.user_id = userID 确保数据隔离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | GET /invitations/my | mitigate | service 层 WHERE 条件绑定 userID（从 JWT 解析），用户只能查看自己的数据 |
| T-03-09 | Spoofing | type 参数篡改 | accept | type 参数仅控制查询维度（created/joined），无论哪个值都只返回当前用户相关数据，无安全风险 |
</threat_model>

<verification>
1. `cd backend && go build ./...` 编译通过
2. 启动后端，`curl "http://localhost:8080/api/v1/invitations/my?type=created" -H "Authorization: Bearer <token>"` 返回 200 + {list, total}
3. type=joined 返回参与但非自己创建的邀约
4. 前端构建无错误
5. 个人中心点击"我的邀约"跳转到新页面
6. Tab 切换正常，各 Tab 独立加载数据
</verification>

<success_criteria>
1. 后端 GET /invitations/my 接口正确区分 created 和 joined 查询
2. 前端我的邀约页面 Tab 切换正常
3. 卡片使用 status 模式显示状态标签
4. 个人中心菜单有"我的邀约"入口
5. 路由注册正确，页面可正常访问
</success_criteria>

<output>
创建 `.planning/phases/03-nearby-and-my-invitations/03-03-SUMMARY.md`
</output>
